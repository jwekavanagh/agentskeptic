---
surfaceKind: example
title: Example verified workflow wf_complete — AgentSkeptic
description: Static public example bundled wf_complete workflow result and human truth report with read-only SQL aligned to structured tool activity at verification time.
intent: Developers who want a committed public envelope that matches the bundled success path from npm start.
valueProposition: You can diff your local runs against this envelope before wiring your own registry and database.
primaryCta: integrate
route: /examples/wf-complete
evaluatorLens: false
embedKey: wf_complete
---

# Bundled verified workflow (wf_complete)

Bundled wf_complete demo: read-only SQL matched the expected contacts row under the packaged registry at verification time.

This page is for **organic discovery**: it shows the same bundled success path you get from `npm start`, rendered as a public verification envelope. For wiring read-only SQL on your own database, use [`/integrate`](/integrate).

## What to do next

- Run first-run on [`/integrate`](/integrate) to reproduce wf_complete on your database.
- Compare the failure envelope on [`/examples/wf-missing`](/examples/wf-missing).
- Read more guides on [`/guides`](/guides).
- Review [`/security`](/security) before sharing database-backed reports externally.
