{
  "schemaVersion": 2,
  "certificate": {
    "schemaVersion": 1,
    "workflowId": "wf_fixture_share_v2",
    "runKind": "contract_sql",
    "stateRelation": "does_not_match",
    "highStakesReliance": "prohibited",
    "relianceRationale": "At least one step failed verification against the database (missing row, wrong values, or partial multi-effect failure). Do not treat this run as meeting its intended persisted outcome.",
    "intentSummary": "NOT TRUSTED: At least one step failed verification against the database (determinate failure).",
    "explanation": {
      "headline": "Fixture headline for share POST v2",
      "details": [{ "code": "ROW_ABSENT", "message": "Expected row is missing." }]
    },
    "steps": [
      {
        "seq": 0,
        "toolId": "crm.upsert_contact",
        "declaredAction": "Upsert contact",
        "expectedOutcome": "contacts.id = x",
        "observedOutcome": "0 rows"
      }
    ],
    "humanReport": "workflow_id: wf_fixture_share_v2\nworkflow_status: inconsistent\ntrust: NOT TRUSTED\n"
  }
}
